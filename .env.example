# ============================================================
# Garry n8n Decision Action — Environment Variables
# ============================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# All VITE_ prefixed variables are exposed to the browser.
# Do NOT put secrets that should stay server-side here.
# ============================================================

# --- App -----------------------------------------------------------

# Log level for the structured logger.
# Values: debug | info | warn | error
# Default: "debug" in development, "info" in production.
VITE_LOG_LEVEL=

# --- n8n Integration ----------------------------------------------
#
# SECURITY: If set, VITE_N8N_API_KEY is visible in the browser. Prefer
# protecting your n8n instance with auth or calling it via a backend proxy.

# n8n instance URL
VITE_N8N_URL=http://localhost:5678

# n8n API key (if using API authentication; avoid for production — use backend proxy)
VITE_N8N_API_KEY=

# --- Build ---------------------------------------------------------

# Node environment (set automatically by Vite).
# Values: development | production | test
# NODE_ENV=development
